# Normalize line endings to LF in the repo, checked out as-is on all platforms
* text=auto eol=lf

# Shell scripts must be LF — CRLF breaks bash
*.sh text eol=lf

# Windows binaries — no line ending conversion
*.exe binary
*.vsix binary
